# TEMPLATE ONLY — do not store real secrets in source control.
# Use Sealed Secrets (https://github.com/bitnami-labs/sealed-secrets) or
# Azure Key Vault + External Secrets Operator to inject real values at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: sqlmcpserver-secrets
  namespace: sqlmcpserver
  labels:
    app.kubernetes.io/name: sqlmcpserver
type: Opaque
stringData:
  # Base64-encode or use Sealed Secrets for these values
  SqlServer__ConnectionString: "REPLACE_WITH_SEALED_SECRET"
  Cache__RedisConnectionString: "REPLACE_WITH_SEALED_SECRET"
  KeyVault__Uri: ""
